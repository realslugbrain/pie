struct User:
    id: int
    name: string
end

fn take(value: User):
    println(value.name)
end

fn main() -> int:
    user: User -> new User(id: 1, name: "Simon")
    take(user)
    println(user.name)
    return 0
end

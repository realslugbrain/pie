fn add(a: int, b: int) -> int:
    return a + b
end

fn choose(flag: bool, yes: int, no: int) -> int:
    if flag:
        return yes
    else:
        return no
    end
end

fn main() -> int:
    println("params=", add(20, 22), " ", choose(true, 7, 9))
    return 0
end

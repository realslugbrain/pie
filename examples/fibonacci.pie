fn fibonacci(n: int) -> int:
    if n <= 1:
        return n
    end
    return fibonacci(n - 1) + fibonacci(n - 2)
end

fn main() -> int:
    for n in 0..=10:
        println("fib(", n, ") = ", fibonacci(n))
    end
    return 0
end
